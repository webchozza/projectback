<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="main">

	<select id="allList" resultType="first.com.model.BoardDTO">
	
	</select>
	
	<select id="allSearchList" resultType="first.com.model.BoardDTO" parameterType="java.lang.String">
	SELECT * FROM board WHERE BOARD_TITLE like '%'||#{search}||'%' or 
	BOARD_CONTENT like '%'||#{search}||'%' or BOARD_NICKNAME like '%'||#{search}||'%' ORDER BY board_date DESC
	</select>




</mapper>