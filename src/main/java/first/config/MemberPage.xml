<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memberpage">

	 <select id="myBoardList" resultType="first.com.model.BoardDTO" parameterType="java.util.Map">
	 SELECT * FROM board WHERE member_id = #{member_id} and 
	 board_title like '%'||#{search}||'%' OR board_content like '%'||#{search}||'%' ORDER BY board_date DESC
	 </select>

</mapper>